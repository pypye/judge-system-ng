import { VerdictFrExcPipe } from './verdict-fr-exc.pipe';

describe('VerdictFrExcPipe', () => {
  it('create an instance', () => {
    const pipe = new VerdictFrExcPipe();
    expect(pipe).toBeTruthy();
  });
});
